## 2. `datasets/train_test/test/README.md`

```markdown
# Test Dataset

This folder is used to store the test hyperspectral images for model evaluation.

Due to the large file size, the original test data are not included in this repository.  
Please prepare the dataset manually and place the corresponding `.mat` files in this folder before running the test script.

Expected structure:

```text
test/
├── scene1.mat
├── scene2.mat
├── ...